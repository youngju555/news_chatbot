# 1. 보안 파일 (가장 중요!)
.env
.secrets
secrets.toml

# 2. Python 가상환경 폴더 (용량이 크므로 제외)
venv/
env/
.venv/

# 3. Python 컴파일/캐시 파일 (불필요)
__pycache__/
*.pyc
*.pyo
*.pyd

# 4. 운영체제 자동 생성 파일
.DS_Store
Thumbs.db

# 5. IDE 설정 파일 (협업 시 충돌 방지)
.vscode/
.idea/

# 6. Streamlit 관련 (로컬 설정 제외)
.streamlit/